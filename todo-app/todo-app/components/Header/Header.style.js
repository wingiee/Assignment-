import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    wrapper: {
        alignItems: 'center',
        marginBottom: 15
    },
  logo: {
    width: 100,
    height: 100
  },
  title: {
    fontSize: 30,
    fontWeight: 'bold',
    marginTop: 10,
    color: '#2684FF'
  }
});